{
  "name": "qoder-model-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "test:manager": "node --test tests/*.test.mjs",
    "build": "node scripts/qoder-gateway.mjs build"
  }
}
